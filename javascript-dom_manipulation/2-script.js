const header = document.getElementById('red_header');
header.addEventListener('click', function onClick (event) {
  // Add a class (red) to a given element on click
  header.classList.add('red');
});
